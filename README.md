# mdeditor

Typora 式 WYSIWYG markdown 编辑器组件库（重写中）。

- `packages/core` — `@mdeditor/core`：headless 编辑器内核（Milkdown/ProseMirror）
- `packages/theme` — `@mdeditor/theme`：CSS variables 设计令牌 + 亮/暗主题

## 开发

```bash
pnpm install
pnpm build
pnpm test
```
